import { StyleSheet } from 'react-native'

export const s = StyleSheet.create({
  block: { gap: 16, width: '100%' },
  container: {
    gap: 12,
    flexDirection: 'row',
  },
  radioButton: {
    width: 24,
    height: 24,
    borderRadius: 50,
  },
  labelText: {
    fontSize: 15,
    lineHeight: 24,
    fontWeight: '500',
  },
})
