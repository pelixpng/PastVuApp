# Google Maps key for Android. Must allow the signing certificate of the build:
# the release keystore for Play, the debug keystore for local builds.
GOOGLE_MAPS_API_KEY=
# LocationIQ key for place search.
PLACE_API_KEY=
